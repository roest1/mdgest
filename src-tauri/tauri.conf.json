{
  "$schema": "https://schema.tauri.app/config/2",
  "productName": "mdgest",
  "version": "0.2.0",
  "identifier": "dev.mdgest.desktop",
  "build": {
    "beforeDevCommand": { "script": "bun run dev", "cwd": "../web" },
    "devUrl": "http://127.0.0.1:5173",
    "beforeBuildCommand": { "script": "bun run build", "cwd": "../web" },
    "frontendDist": "../web/dist"
  },
  "app": {
    "windows": [
      {
        "label": "main",
        "title": "mdgest",
        "width": 1500,
        "height": 950,
        "minWidth": 900,
        "minHeight": 600,
        "dragDropEnabled": true
      }
    ],
    "security": {
      "csp": "default-src 'self'; script-src 'self'; style-src 'self' 'unsafe-inline'; font-src 'self' data:; connect-src 'self' ipc: http://ipc.localhost http://127.0.0.1:* http://localhost:*; img-src 'self' http://127.0.0.1:* http://localhost:* data: blob:",
      "devCsp": "default-src 'self' 'unsafe-inline' data: blob: http://127.0.0.1:* http://localhost:*; connect-src 'self' ipc: http://ipc.localhost http://127.0.0.1:* http://localhost:* ws://127.0.0.1:* ws://localhost:*; img-src 'self' http://127.0.0.1:* http://localhost:* data: blob:"
    }
  },
  "bundle": {
    "active": true,
    "targets": "all",
    "externalBin": ["binaries/mdgest-engine"],
    "icon": [
      "icons/32x32.png",
      "icons/128x128.png",
      "icons/128x128@2x.png",
      "icons/icon.icns",
      "icons/icon.ico"
    ],
    "category": "Productivity",
    "shortDescription": "pdf → markdown, page by page",
    "longDescription": "Converts PDFs into reviewable markdown: the page on the left, its markdown on the right, every piece of text and every picture a numbered, draggable, shapeable block. Works completely offline; ships the mdgest CLI inside the same engine binary."
  }
}
