{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "the main window: core events (drag-drop), native dialogs",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
