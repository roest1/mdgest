# Only mdask / mdcite (not built yet) need a model. Same names as mdgest v1.
# LLM_HOST=https://your-litellm-gateway            # OpenAI-compatible, /v1 is appended
# LLM_API_KEY=
# LLM_MODEL=openai/gpt-oss-20b
# Where the workspace lives (default: ./workspace next to the Makefile)
# MDGEST_WORKSPACE=/path/to/workspace
